## 1. Update site/index.html

- [x] 1.1 Add four new `<li>` entries to the "Available now" `<ul>` (Scheduled Sync, Drift detection, Unattended Terraform execution, Auto-reconciliation), using the approved copy recorded in the issue #46 alignment comment, matching the existing bold-name em-dash-description style
- [x] 1.2 Update the `<meta name="description">` tag to the approved replacement text
- [x] 1.3 Verify the "On the roadmap" `<ul>` is left byte-for-byte unchanged

## 2. Update openspec/specs/site/spec.md

- [x] 2.1 Sync the "Available-now features are only ones with real implementation" scenario to name all six shipped capabilities, per this change's specs delta

## 3. Verify

- [x] 3.1 Open site/index.html in a browser and confirm the "Available now" list names all six capabilities and the roadmap section reads as before
